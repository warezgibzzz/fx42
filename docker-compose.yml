version: "3.9"  # optional since v1.27.0
services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: fx42
      POSTGRES_DB: fx42
      POSTGRES_USER: fx42